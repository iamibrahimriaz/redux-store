export const incrementFamilyCount = () => ({
	type: 'INCREMENT_FAMILY_COUNT',
  });
  
  export const decrementFamilyCount = () => ({
	type: 'DECREMENT_FAMILY_COUNT',
  });
  
  export const incrementFriendsCount = () => ({
	type: 'INCREMENT_FRIENDS_COUNT',
  });
  
  export const decrementFriendsCount = () => ({
	type: 'DECREMENT_FRIENDS_COUNT',
  });
  
  export const incrementRelativesCount = () => ({
	type: 'INCREMENT_RELATIVES_COUNT',
  });
  
  export const decrementRelativesCount = () => ({
	type: 'DECREMENT_RELATIVES_COUNT',
  });
  