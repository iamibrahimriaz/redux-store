export const getFamilyCount = (state) => state.personal.familyCount;

export const getFriendsCount = (state) => state.personal.friendsCount;

export const getRelativesCount = (state) => state.personal.relativesCount;
